{
    "type": "object",
    "additionalProperties": false,
    "description": "A reference to a user who committed a project component",
    "properties": {
        "id": {
            "type": "string",
            "description": "The user id from whatever user service is being used"
        },
        "email": {
            "type": "string",
            "description": "the user's email address. Serves as the human-readable username"
        }
    }
}