{
  "id": "#/spidamin/asset/asset/service_and_ids.schema",
  "description": "A list of services and stationIds in those services to link together.",
  "type": "object",
  "additionalProperties": {
		"type": "array",
		"description": "Each property is a service name and the property values are an array of stationIds.",
		"items": { "type": "string" }
	}
}
