{
  "description": "Note: these used to be INITIAL and FINAL. INITIAL is now DYNAMIC, FINAL is now STATIC. NONLINEAR_STRESS_STRAIN is 'polynomial'.",
  "enum": [
    "STATIC",
    "DYNAMIC",
    "NONLINEAR_STRESS_STRAIN"
  ],
  "type": "string"
}
