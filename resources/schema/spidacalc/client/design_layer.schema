{
  "description": "Design layer(s) can be added to locations.",
  "id": "#/spidacalc/client/design_layer.schema",
  "type": "object",
  "required": [
    "name",
    "type"
  ],
  "properties": {
      "name": {
            "description": "Name for the design",
            "type": "string"
      },
      "type": {
        "type": "string",
        "enum": [
          "Measured",
          "Theoretical",
          "Recommended"
         ]
      }
  }
}
