{
    "extends": {
        "$ref": "rpc_asset"
    },

public JSONString findStationsFromAdvancedSearch(JSONString advancedSerach);
    public JSONString findCompanyStationsFromAdvancedSearch(long companyID, JSONString advancedSerach);
    public JSONString getSearchableKeys();
    
    "getPossibleAssetTypes": {
        "description": "Analyze given pole and wait for results",
        "returns": {
            "type": "array",
            "name": "types",
            "required": true,
            "description": "Asset types in this service",
            "items": {
                "type": "string"
            }
        },
        "params": [
            {
                "type": "number",
                "name": "companyId",
                "required": true,
                "description": "Company to get the asset types for."
            }
        ]
    }
}